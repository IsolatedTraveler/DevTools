console.log('加载amis->opreate')
let api = require('./api'), button = require('./button'), buttonGroup = require('./buttonGroup'), crud = require('./crud'), date = require('./date'), 
dateRange = require('./dateRange'), dialog = require('.//dialog'), echart = require('./echart'), form = require('./form'), group = require('./group'),iframe = require('./iframe'), number = require('./number'),
operation = require('./operation'), select = require('./select'), service = require('./service')
text = require('./text'), textarea = require('./textarea')
console.log('加载amis->opreate成功')
module.exports = {
  api,
  button,
  buttonGroup,
  crud,
  date,
  dateRange,
  dialog,
  echart,
  form,
  group,
  iframe,
  number,
  operation,
  select,
  service,
  text,
  textarea
}